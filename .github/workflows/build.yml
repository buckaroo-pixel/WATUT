name: Build

on:
  push:
    branches: [ 1.21.11 ]

jobs:
  build-fabric:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout WATUT
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Clone CoroUtil
        run: git clone --branch 1.21.11 --depth 1 https://github.com/Brskt/CoroUtil.git ../CoroUtil

      - name: Build CoroUtil (Fabric) and publish to mavenLocal
        working-directory: ../CoroUtil
        run: ./gradlew publishToMavenLocal

      - name: Build WATUT (Fabric)
        run: ./gradlew -b build_fabric.gradle build

      - name: Upload Fabric JAR
        uses: actions/upload-artifact@v4
        with:
          name: watut-fabric
          path: build/libs/watut-fabric-*.jar

  build-neoforge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout WATUT
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Clone CoroUtil
        run: git clone --branch 1.21.11 --depth 1 https://github.com/Brskt/CoroUtil.git ../CoroUtil

      - name: Build CoroUtil (NeoForge) and publish to mavenLocal
        working-directory: ../CoroUtil
        run: ./gradlew -b build_neoforge.gradle publishToMavenLocal

      - name: Build WATUT (NeoForge)
        run: ./gradlew -b build_neoforge.gradle build

      - name: Upload NeoForge JAR
        uses: actions/upload-artifact@v4
        with:
          name: watut-neoforge
          path: build/libs/watut-neoforge-*.jar
